# Codex for Open Source Application Draft

Official form:

```text
https://openai.com/ja-JP/form/codex-for-oss/
```

Official program page:

```text
https://developers.openai.com/community/codex-for-oss
```

## Fields

Last name:

```text
[enter legal last name]
```

First name:

```text
[enter legal first name]
```

Email:

```text
[enter ChatGPT account email]
```

GitHub username:

```text
toaruseigyoya
```

GitHub repository URL:

```text
https://github.com/toaruseigyoya/plc-field-tools
```

Role:

```text
Main maintainer
```

Why this repository is eligible, 500 characters max:

```text
PLC Field Tools is an offline-first OSS toolkit for control engineers and maintenance teams. It helps clean I/O lists, validate PLC tags, calculate subnets, normalize alarm CSVs, and turn field notes into checklists without uploading factory data. It targets a practical niche in the automation ecosystem where small, auditable tools reduce repeated maintenance work.
```

Interested items:

```text
Codex Security
API credits for the project
```

OpenAI organization ID:

```text
[enter OpenAI organization ID from platform.openai.com]
```

How API credits will be used, 500 characters max:

```text
API credits will be used for maintainer workflows: reviewing pull requests, triaging issue reports, generating tests from sanitized examples, checking documentation changes, and preparing release notes. The production app will remain offline-first; API use is for OSS maintenance automation and code review, not for sending user field data to a backend.
```

Anything else, 500 characters max:

```text
The project is intentionally small and transparent so field engineers can audit it before using it with sensitive plant data. Codex would help keep the repository maintained through test generation, review, security checks, and issue triage while the tool grows toward vendor-specific presets and Japanese UI support.
```

